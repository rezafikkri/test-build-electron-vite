name: Build Electron App
on:
  push:
    branches: [main]

env:
  GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
  
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Clean install dependencies
        run: npm ci

      - name: Build
        run: npm run make

      - uses: actions/upload-artifact@v4
        with:
          name: build_release
          path: /home/runner/work/test-build-electron-vite/test-build-electron-vite/out/make/deb/x64/test-build-electron-vite*.deb

  build-and-release:
    runs-on: windows-latest

    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'npm'

      - name: Clean install dependencies
        run: npm ci

      - name: Build
        run: npm run make

      - uses: actions/download-artifact@v4

      - name: Display structure of downloaded files
        shell: bash
        run: ls -R
